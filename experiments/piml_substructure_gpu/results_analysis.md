# 子结构 PIML GPU 实验结果

## 2026-09-07 基线归属调整

旧 `training_linear_corner_2d_cpu` 已有运行产物，但模型力学误差未通过验收；它是
混合低密度分布下的探索工况，不对应申请书中的 full_trace 精度证据。原 JSON 保留。

新增 `training_full_trace_2d`，复用 examples 的标签、全批量训练、局部误差与 24
子结构全局精度评价。代码已调整，尚未运行训练或测试，也尚未确认历史精度复现。
只有复核局部、位移、柔度误差和回退数量后，才能判断结果是否可用于性能结论。

其他六个阶段尚无独立性能 case。
