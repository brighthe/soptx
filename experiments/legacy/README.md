# Legacy research drivers

这里保存从旧包目录迁出的研究运行脚本和 package archive 快照。它们可能使用已失效
的导入路径、交互式绘图、GPU 或长时间求解，不进入 CI，也不声明为当前可运行基线。

`package_archive/soptx` 保存不属于 `1.1.x` 公共兼容承诺的 `old`、`backup` 以及
已由薄转发替代的完整旧实现。后续归档清理以授权后的 `archive/pre-v2` tag、Git
历史和本目录快照为准。
